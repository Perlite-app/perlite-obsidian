node_modules/
main.js
main.js.map
*.tsbuildinfo

# Local-only Claude Code project context — not a deliverable, not assumed to exist from
# a fresh clone. Mirrors the native app repo's own `claude-docs/` convention exactly.
claude-docs/

# Obsidian plugin dev loop — a scratch vault symlinking this repo's build output into
# .obsidian/plugins/perlite/, gitignored per the plugin plan's own Wave 0 instructions.
dev-vault/

.DS_Store
